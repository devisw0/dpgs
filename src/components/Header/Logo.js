import React from 'react';
import Typography from '@mui/material/Typography';

function Logo() {
  return (
    <Typography variant="h6" sx={{ fontWeight: 700, textTransform: 'uppercase', letterSpacing: 2, color: 'primary.main' }}>
      DPGS Online Shop
    </Typography>
  );
}

export default Logo; 